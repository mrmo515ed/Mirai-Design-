<?xml version="1.0" encoding="utf-8"?>
<!-- CINEMATIC HERO SCRIM — the only large gradient in the system.
     Bottom-anchored image legibility: transparent → 60% → bg. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:angle="90"
        android:startColor="@color/mirai_scrim_hero_bottom"
        android:centerColor="@color/mirai_scrim_hero_mid"
        android:endColor="@android:color/transparent"
        android:type="linear" />
</shape>
