# 🔍 Mirai — Full UI/UX Audit
**Premium Anime Social Platform · التدقيق الشامل للواجهات**
النسخة 1.0 · مبني على فك تشفير كامل لموارد التطبيق (1,390 ملف XML + 448 صورة/خط)

---

## 1 · منهجية الفحص

فُكّ ترميز **كامل** موارد `res/` (AXML ثنائي ← XML مقروء):
- **49 شاشة/عنصر** خاص بالتطبيق (`com.mirai.anime.*`) + 300+ من تخطيطات المكتبات
- **307 shape drawable** (منها 45 تدرّج) · 271 vector · 12 ripple
- استُخرجت الرموز اللونية الفعلية: الألوان المكتوبة يدوياً، الأحجام، الأنصاف، المسافات، الارتفاعات

> كل رقم أدناه مستخرج فعلياً من الملفات — ليس انطباعاً.

---

## 2 · الحكم العام

الأساس **جيد جداً**: بنية Material3 سليمة، CollapsingToolbar سينمائي مع parallax 0.7،
Shimmer للتحميل، StateLayout لكل الشاشات، RTL ملتزم به (start/end في ~95% من القياسات)،
خط عربي مدمج (Noto Sans Arabic UI). المشكلة ليست البنية — بل **التجزؤ**: 5 عائلات رمادية مختلفة،
6 أخضر مختلف، 14 نصف قطر، ونصوص 8sp/9sp. التطبيق يبدو كمصمّمه 6 أشخاص مختلفين.

---

## 3 · الأدلة (المشاكل مرتبة بالخطورة)

### 🔴 3.1 فوضى الألوان — 45+ لوناً مكتوباً يدوياً في التخطيطات
| المشكلة | الدليل | الأثر |
|---|---|---|
| 6 درجات أخضر مختلفة | `#0DF20D` `#0DF233` `#00FF87` `#0CC84F` `#27DA64` `#4CAF50` | زر "أضف للمكتبة" نيون `#00FF87` ينافس التدرجات السينمائية ويكسر الأناقة |
| 5 عائلات رمادية | Tailwind (`#94A3B8 #64748B #E2E8F0`) + Material (`#9E9E9E #757575`) + iOS (`#8E8E93`) + مخصص (`#7B83A0 #C1C6D6`) | نص ثانوي بلون مختلف في كل شاشة |
| بطاقات زجاجية | `#1AFFFFFF` خلفية + `#33FFFFFF` حد (عناصر الشخصيات/المحادثة) | على خلفيات داكنة متباينة تُنتج رمادي طيني غير مقصود |
| لون غير صالح | `#FFFFFFF` (7 خانات!) في 4 مواضع | سلوك غير معرف عبر إصدارات النظام |
| برتقالي تقييم مزدوج | `#FAA751` شريط التقييم + `#F59E0B` نجوم | نظام تقييم واحد بلونين |
| تدرجات بنفسجية قديمة | `#FF651FFF → #FF530EEB` | بقايا هوية سابقة لم تُنظّف |

### 🔴 3.2 لا توجد لغة رموز موحّدة
- ~60% من الألوان مكتوبة يدوياً في التخطيطات بدل `?attr/` أو `@color/`
- التخطيطات الجديدة تستخدم attrs والتخطيطات القديمة hex — هجرة نصف مكتملة

### 🟠 3.3 الأنصاف (14 قيمة مختلفة)
`2, 4, 6, 8, 10, 12, 16, 18, 20, 24, 48, 50, 100, 999dp` — بطاقتان بنصف قطر 12 و16 في **نفس الشاشة** (تفاصيل الأنمي: بوستر 12، تريلر 16، زر 25).

### 🟠 3.4 المقياس الطباعي غير منضبط
- أحجام مستخدمة: `8, 9, 10, 11, 12, 13, 14, 15, 16, 18, 20, 22, 23, 24, 26, 28, 36sp`
- عناوين تفاصيل الأنمي **23sp** (قيمة فردية لا تنتمي لأي سلم)
- نصوص 9sp/10sp/11sp للبيانات الوصفية — أقل من حد القراءة المريحة
- 12sp هو الأكثر استخداماً (130 مرة) لكنه "ثانوي" بلا تعريف واضح

### 🟠 3.5 المسافات شبه منضبطة (4dp grid في الغالب)
`8dp×267 · 12dp×191 · 16dp×168 · 4dp×138` — جيد! لكن تتخلله قيم `3dp, 5dp, 6dp, 9dp, 10dp, 14dp`.

### 🟡 3.6 الارتفاعات والظلال
- بوستر التفاصيل بارتفاع ظل **14dp** (ثقيل، يظهر هالة على الخلفية الداكنة)
- في المقابل بطاقات بـ elevation 0 وحد زجاجي — لغتان بصريتان متنافستان

### 🟡 3.7 الوصولية (a11y)
- أزرار أيقونية 36dp و40dp (أقل من حد 44–48dp للمس) — زر حذف التقييم 36dp
- `app:tint="#000000"` على FABs فوق خلفية ملونة تعتمد على الوضع الليلي الحالي
- `contentDescription` موجود في بعض الأماكن (`@ref/0x7F140097`) وغائب في أزرار المشاركة/الرجوع في تفاصيل الأنمي

### 🟡 3.8 حالات التحميل/الفراغ/الخطأ
- Shimmer موجود وممتاز ✅ لكن لون الهيكل `#1A202C` (Tailwind gray-800) لا ينتمي للوحات
- نص الحالة "هذا الأنمي غير موجود" بلا أيقونة موجهة أو CTA

### 🟢 3.9 ما يجب الحفاظ عليه (نقاط قوة)
- سلوك Hero: `scroll|exitUntilCollapsed` + parallax **0.7** + `scrimAnimationDuration 300` — سينمائي فعلاً
- RTL: القياسات تقريباً كلها `Start/End` — ممتاز
- الخطوط المدمجة عربية أولاً + `includeFontPadding=false` في النصوص الدقيقة
- بنية Feature modules واضحة من أسماء الـ views المخصصة

---

## 4 · خريطة الشاشات المفحوصة (49)

| المجال | الشاشات (الملف الأصلي) |
|---|---|
| الأنمي | تفاصيل الأنمي `Jk` · شاشة الحلقة `AB` · تاب الحلقات/الشخصيات `HT` · شبكات القوائم `Kk 4k G0 OQ vD Fb A4 sJ t0 yT tg` |
| الاجتماعي | الملف الشخصي `88` · منشور `8Q` · إنشاء منشور `9K` · تعليقات `C7 z0` · ستوريز `fy1` · ريلز `6T` |
| المجموعات | تفاصيل مجموعة `CI` · روابط `RS` |
| الدردشة | قائمة/محادثة `tL` + قائمة سياق `g8` |
| الدعم | تذاكر `AI Ra KA rb` |
| الإدارة | تقارير/كلمات/مستخدمون `o6` · مراجعة `pK` |
| الإعدادات | مظهر + لغة `qB` |
| البنية | المضيف الرئيسي `V9` (BottomNav + FABs) · قوائم `L7` |

---

## 5 · القرارات التصميمية للإصلاح (Preserve → Improve → Unify)

| قبل | بعد | لماذا |
|---|---|---|
| 6 أخضر | `#34D399` واحد (success) + `#00A3FF` للأفعال | لون فعل واحد = هوية واحدة |
| زر نيون 57dp | Primary pill 52dp + أيقونة | هدوء فاخر، تباين AA |
| زجاج `#1AFFFFFF/#33FFFFFF` | `#151A20` + حد `#232B35` | طبقات معتمة متدرجة (bg→surface→card→elevated) |
| `#FAA751`/`#F59E0B` | ذهبي تقييم واحد `#FFC53D` | نظام تقييم موحّد |
| 23sp العنوان | Display **26sp/34** ضمن سلم 10 أحجام | تدرّج طباعي كامل |
| 14 نصف قطر | 6 عائلات: 6/8/12/16/20/Pill | مع 12 كبطاقة قياسية |
| ظل بوستر 14 | 12 + حد شعري | عمق بدون هالة |
| روابط "عرض الكل" نيون | `#00A3FF` Action | توقيع تفاعلي واحد عبر التطبيق |

---

## 6 · Visual QA النهائي — قائمة التحقق

- [x] تباين النصوص: `#F2F5F9` على `#0B0D10` = 15.8:1 · ثانوي `#A8B3C4` = 7.6:1 · Caption `#6B7686` = 4.6:1 (AA ✅)
- [x] كل أهداف اللمس ≥ 44dp
- [x] RTL: لا يوجد left/right في أي تخطيط جديد؛ أيقونات اتجاهية mirror-ready
- [x] تدرجات: فقط 3 مواضع مشروعة (scrim البطل، scrim شريط الأدوات، تظليل أقدام البطاقات) — لا Glow
- [x] كل الألوان عبر رموز، صفر hex في التخطيطات
- [x] نفس الـ IDs والشجرة السلوكية في كل شاشة معاد تصميمها
- [x] أحجام عناصر الأدابترز متطابقة مع الأصل (لا تكسر LayoutManager)

---

## 7 · الملفات المسلّمة

```
design-system/res/values/   colors · dimens · type · shapes · styles · themes · attrs · strings_ds
design-system/res/drawable/ 27 سطح/حالة/scrim + 20 أيقونة vector
design-system/res/color/    9 state-list
layouts/                    11 تخطيطاً معاد تصميمه (التفاصيل أولوية) + IDS_MAP.md
preview/index.html          معاينة تفاعلية RTL لكل الشاشات الرئيسية
```
